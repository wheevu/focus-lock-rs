{
  "name": "focus-lock-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 1420 --strictPort",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "cd .. && npx --prefix ui tauri --config src-tauri/tauri.conf.json",
    "tauri:dev": "cd .. && npx --prefix ui tauri dev --config src-tauri/tauri.conf.json",
    "tauri:build": "cd .. && npx --prefix ui tauri build --config src-tauri/tauri.conf.json"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.5.0",
    "@tauri-apps/plugin-dialog": "^2.2.0",
    "@tauri-apps/plugin-fs": "^2.4.0",
    "@tauri-apps/plugin-shell": "^2.2.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "@tauri-apps/cli": "^2.10.0",
    "svelte": "^5.28.2",
    "typescript": "^5.8.3",
    "vite": "^6.3.3"
  }
}
